node_modules
out
dist
release
*.log
.DS_Store
.env
.env.local

*.tsbuildinfo
# portable gh CLI lives in the repo-root tools/ — anchor to root so it does NOT
# also ignore the source dir src/main/agent/tools/
/tools/
